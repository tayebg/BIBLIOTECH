# Database Configuration
# Options: postgresql (recommended) or sqlite
DB_TYPE=postgresql

# PostgreSQL Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bibliotech
DB_USER=postgres
DB_PASSWORD=your_password_here

# SQLite Fallback Configuration
SQLITE_PATH=bibliotheque.db
